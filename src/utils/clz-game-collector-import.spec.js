describe('CLZ Game Collector Import', () => {
  it('loads XML data exported from CLZ app and transforms to JSON', () => {

  })

  it('allows transform/distillation of just an array of all game titles', () => {

  })
})